{
  "name": "claude-code-export",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:web": "bun --filter web dev --watch",
    "start:web": "bun --filter web start",
    "test": "bun test",
    "check": "tsc --noEmit -p apps/web"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
